spring:
  application:
    name: vaultroom
  cors:
    originPatterns: ${SPRING_CORS_ORIGINS}
  jackson:
    property-naming-strategy: SNAKE_CASE
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    baseline-on-migrate: true
